import 'package:intl/intl.dart';

DateTime firstDate = DateTime(1980);
DateTime lastDate = DateTime(DateTime.now().year + 20);
DateFormat defaultDate = DateFormat('dd/MM/yyyy');
DateFormat isoDateFormat = DateFormat('yyyy-MM-dd');
